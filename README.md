Login form demo

Open `index.html` in your browser to try the login form.

Notes:
- Client-side validation is implemented in `script.js`.
- Replace the simulated server call with a real `fetch('/login', { method: 'POST', body: JSON.stringify(...) })` to integrate with a backend.

Example test credentials (simulated):
- email: user@example.com
- password: password
